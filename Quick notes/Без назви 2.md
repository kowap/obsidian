
|               |              |
| ------------- | ------------ |
| bonus-details | `bonus`      |
| comments      | `comments`   |
| faq           | `content`    |
| footer        | `navigation` |
| review-hero   | `content`    |
| top-games     | `games`      |
Нужно немного переделать модуль промптов /generator/prompts. у категории так же должен быть slug, а у всех промптов возмодность назначать типы, вот список, он зарезервированый: `bonus`   `comments`  `content`   `navigation`  `content`    `games`  . у одного промпта может быть больше чем один тип. ты можешь выполнять сам миграции, проект крутится в докер контейнерах